[package]
name = "kamimusuhi-resident"
description = "Always-on resident node daemon: health, routing, spool/NAS sync and K-CORE supervision"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lib]
name = "kamimusuhi_resident"
path = "src/lib.rs"

[[bin]]
name = "kamimusuhi"
path = "src/main.rs"

[dependencies]
kamimusuhi-resource-http = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
# Online snapshot of the canonical SQLite store (`VACUUM INTO`).
rusqlite = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
