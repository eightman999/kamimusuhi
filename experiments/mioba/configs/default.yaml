experiment: {id_prefix: "M", runs_dir: "experiments/mioba/.runs"}
population: {target_size: 64}
evolution: {max_generations: 1, elite_k: 8, mutation_seed: 12345}
worker: {batch_size: auto, candidates: [1,2,4,8,16,32], heartbeat_s: 5, lost_after_s: 90, grace_s: 30}
jobs: {requeue_unknown: true, max_attempts: 3}
evaluation: {tier: smoke, duration_ms: 500, backend: torch, environment_id: synthetic-quiet-v0, target_rate_hz: 5.0}
fba: {data_dir: null, synthetic: true, synthetic_neurons: 2000}
checkpoint: {interval_s: 600}
stop: {timeout_s: 120}
mie: {interval_s: 5, collectors: [gpu, cpu, ram], anomaly: {gpu_temp_c: 85}}
env: {stim_fraction: 0.01, stim_rate_hz: 50}
traces: {store_full_for: [elite, novel, debug, explicit], dir: traces}
gui: {enabled: true}
