# Copy to /srv/kamimusuhi/config/secrets.env (mode 600). NEVER commit the real file.
# Shared bearer token between the Pi and llm_master residents (same value on both).
KAMIMUSUHI_NODE_TOKEN=
# HAI API key (https://hai-api.hcloud.ltd).
HAI_API_KEY=
